var p = true
var q = false

function baz(self, xx) {
    var x = 0
    var y = 1

    function qux(y) {
        if (x > y) {

            x = -1//comment after asign

        }
    }

    for (x of xx) {//after for

        self.p = x == 2
    }
    count = count + 1
    while (x <= 0) {
        if (self.p) {

            xx[0] = xx[1]
            self.p = !self.p
            x = x + 1
        } else if (foo("Long"[0]) == 1) {

            self.p = typeof (self) == typeof (null)

        }
    }
    return "Nope"
}

console.log(bar().baz([1, 2]));
while (i <= n) {
    console.log(exp(2, i % 31));
    i = i + 1
}
if (s.length == 8) {
    console.log("adsf");
}
if (true && false) {

    list = [true, false, null]//for this to work needs target implementation in translate

}
state = "nice" ? is_nice : "not nice"
myStr = readlineSync.question()
myStr2 = readlineSync.question()//translate this comment
