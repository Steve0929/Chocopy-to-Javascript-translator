//ii
//abc
var count = 0
var n = 42 //comment

var i = 0

function foo(s) {
    return s.length
//comment after return

}

function foo(s) {
    return true
// x**y

}

function exp(x, y)//pos function declaration comment
//idented comment
{
    var a = 0

    function f(i)//nonlocal a
    {
        function geta() {
            return a * 2

        }

        if (i <= 0) {
//if comment
            return geta(hello)

        } else {

            a = a * x
            return f(i - 1)

        }

    }

    return f(y)

}

class bar extends pp {
    constructor(p, q) {
        this.p = true;
        this.q = false;
    }


    baz(self, xx) {
        global.count


        qux(y)
        {
            var x
            if (x > y) {

                x = -1
//comment after asign

            }

        }
        for (x of xx) {
//after for

            self.p = x == 2
        }
        count = count + 1
        while (x <= 0) {
            if (self.p) {

                xx[0] = xx[1]

                self.p = !self.p

                x = x + 1
            } else if (foo("Long"[0]) == 1) {

                self.p = typeof (self) == typeof (null)


            }
        }
        return "Nope"

    }
}

console.log(bar().baz([1, 2]));
while (i <= n) {
    console.log(exp(2, i % 31));
    i = i + 1
}
if (s.length == 8) {
    console.log("adsf");
}
if (true && false) {

    list = [true, false, null]
//for this to work needs target implementation in translate

}

state = "nice" ? is_nice : "not nice"

myStr = readlineSync.question()

myStr2 = readlineSync.question()
//translate this comment
